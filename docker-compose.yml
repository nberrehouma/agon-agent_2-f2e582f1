version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: password-vault-db
    ports:
      - "27000:27017"
    volumes:
      - password_vault_data:/data/db
    restart: always

volumes:
  password_vault_data:
    name: password_vault_data
